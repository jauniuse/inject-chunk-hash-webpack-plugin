{
  "name": "inject-chunk-meta-plugin",
  "version": "0.0.0",
  "description": "",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "babel lib -d dist",
    "prebuild": "npm run clean",
    "clean": "del-cli dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "del-cli": "^1.1.0"
  }
}
